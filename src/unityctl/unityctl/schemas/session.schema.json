{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mk.example/schemas/unity-agent-bridge/session.schema.json",
  "title": "Unity Agent Bridge Session",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "sessionId",
    "name",
    "projectPath",
    "scenePath",
    "createdAt",
    "startedAt",
    "endedAt",
    "status",
    "trigger",
    "task",
    "failedReason",
    "editorPid",
    "unityVersion"
  ],
  "properties": {
    "sessionId": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}_[0-9]{6}_[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "projectPath": {
      "type": "string",
      "minLength": 1
    },
    "scenePath": {
      "type": ["string", "null"]
    },
    "createdAt": {
      "$ref": "#/$defs/utcTimestamp"
    },
    "startedAt": {
      "anyOf": [
        {
          "$ref": "#/$defs/utcTimestamp"
        },
        {
          "type": "null"
        }
      ]
    },
    "endedAt": {
      "anyOf": [
        {
          "$ref": "#/$defs/utcTimestamp"
        },
        {
          "type": "null"
        }
      ]
    },
    "status": {
      "type": "string",
      "enum": ["created", "running", "stopped", "failed"]
    },
    "trigger": {
      "type": "string",
      "enum": ["agent", "human", "script"]
    },
    "task": {
      "type": "string"
    },
    "failedReason": {
      "type": ["string", "null"],
      "description": "status 为 failed 时的失败原因，例如 compilation_failed / timeout / editor_exited。"
    },
    "editorPid": {
      "type": ["integer", "null"],
      "description": "session 启动时对应的 Unity Editor 进程 ID。"
    },
    "unityVersion": {
      "type": ["string", "null"],
      "description": "session 启动时的 Unity Editor 版本。"
    }
  },
  "$defs": {
    "utcTimestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    }
  }
}
